"""
| Naredba | Rezultat |
| :--- | :--- |
| ```python sync.py``` | Šalje sve (kôd + alati) na tvoj **privatni (private)** GitHub. |
| ```python sync.py --zip``` | Radi samo **Source ZIP** (backup tvog koda) u lokalnom folderu. |
| ```python sync.py --release``` | Čisti kôd i šalje ga na **javni (origin)** GitHub, ali bez uploada ZIP-a. |
| ```python sync.py --deploy``` | **Sve u jednom:** Čisti kôd -> Javni GitHub -> Radi ZIP s DLL-ovima -> GitHub Release. |
"""